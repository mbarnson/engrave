---
phase: 05.1-promote-advn-01-into-v1-scope-for-dorico
plan: 03
subsystem: musicxml
tags: [musicxml, xsd-validation, xmlschema, telemetry, render-pipeline, zip-packaging, music21]

# Dependency graph
requires:
  - phase: 05.1-01
    provides: "MusicXML core data layer (pitch_map, models, builder)"
  - phase: 05.1-02
    provides: "Parallel LilyPond + JSON fan-out with GenerationResult.json_sections"
provides:
  - MusicXML 4.0 XSD validation via xmlschema (validate_musicxml)
  - LilyPond/JSON disagreement telemetry logging (log_disagreement)
  - Per-section JSON to music21 Score assembly (assemble_musicxml)
  - RenderPipeline MusicXML integration with ZIP packaging
  - CLI --no-musicxml flag for render command
  - Vendored MusicXML 4.0 XSD schema files for offline validation
affects: [05.1-04-end-to-end-integration, render-pipeline, cli]

# Tech tracking
tech-stack:
  added: [xmlschema 4.3.1]
  patterns: [graceful-musicxml-degradation, vendored-xsd-validation, lazy-import-in-render-pipeline]

key-files:
  created:
    - src/engrave/musicxml/validator.py
    - src/engrave/musicxml/telemetry.py
    - src/engrave/musicxml/schema/musicxml.xsd
    - src/engrave/generation/json_assembler.py
    - tests/unit/test_xsd_validator.py
    - tests/unit/test_telemetry.py
  modified:
    - src/engrave/rendering/packager.py
    - src/engrave/cli.py
    - tests/unit/test_packager.py
    - pyproject.toml
    - uv.lock

key-decisions:
  - "xmlschema vendored XSD files in src/engrave/musicxml/schema/ for offline validation (14 XSD files from W3C MusicXML 4.0)"
  - "MusicXML validation failure does not remove file from ZIP -- still potentially usable by Dorico"
  - "Lazy import of json_assembler in packager.py to avoid circular dependency and heavy import cost"
  - "assemble_musicxml registers instrument identifiers from both explicit list and section data to handle LLM naming variation"

patterns-established:
  - "Graceful MusicXML degradation: invalid JSON/failed assembly never crashes pipeline, LilyPond output always preserved"
  - "Vendored XSD pattern: schema files checked into repo for deterministic offline validation"
  - "Telemetry file pattern: structured JSON in {output_dir}/telemetry/ for training data collection"

requirements-completed: [ADVN-01]

# Metrics
duration: 5min
completed: 2026-02-25
---

# Phase 05.1 Plan 03: MusicXML Assembly Pipeline Summary

**XSD validation, disagreement telemetry, JSON-to-Score assembly, and RenderPipeline integration with ZIP packaging and --no-musicxml CLI flag**

## Performance

- **Duration:** 5 min
- **Started:** 2026-02-25T03:12:15Z
- **Completed:** 2026-02-25T03:17:15Z
- **Tasks:** 2
- **Files modified:** 11 (including 14 vendored XSD schema files)

## Accomplishments
- MusicXML 4.0 XSD validation via xmlschema with graceful error handling (missing files, malformed XML, invalid schema)
- LilyPond/JSON disagreement telemetry logging to both Python logger and optional structured JSON files for TUNE-02 training data
- Per-section JSON to music21 Score assembly via Pydantic validation and the Plan 01 builder
- RenderPipeline step 6.5 generates MusicXML between compilation and ZIP packaging with full graceful degradation
- ZIP packager now includes *.musicxml alongside *.pdf, *.ly, *.mid
- CLI --no-musicxml flag on render command
- 14 new tests (6 validator + 5 telemetry + 3 packager), 679 total suite tests pass with zero regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: XSD validator, telemetry logger, and JSON assembler** - `0529d37` (feat)
2. **Task 2: RenderPipeline integration, ZIP packaging, and CLI flag** - `eff3c52` (feat)

## Files Created/Modified
- `src/engrave/musicxml/validator.py` - MusicXML 4.0 XSD validation via xmlschema
- `src/engrave/musicxml/telemetry.py` - LilyPond/JSON disagreement logging to logger and JSON files
- `src/engrave/musicxml/schema/` - 14 vendored MusicXML 4.0 XSD schema files from W3C
- `src/engrave/generation/json_assembler.py` - Per-section JSON to music21 Score assembly with Pydantic validation
- `src/engrave/rendering/packager.py` - MusicXML generation step 6.5, include_musicxml param, *.musicxml in ZIP
- `src/engrave/cli.py` - --no-musicxml flag on render command
- `tests/unit/test_xsd_validator.py` - 6 validator tests (valid, invalid, missing file, missing XSD, custom path, malformed)
- `tests/unit/test_telemetry.py` - 5 telemetry tests (logger, file output, no-crash, subdirectory, all fields)
- `tests/unit/test_packager.py` - 3 new tests (musicxml_in_zip, no_musicxml_flag, failure_graceful)
- `pyproject.toml` - Added xmlschema dependency
- `uv.lock` - Lock file updated

## Decisions Made
- Vendored MusicXML 4.0 XSD files (14 files from W3C GitHub) in `src/engrave/musicxml/schema/` for offline validation per RESEARCH.md recommendation
- MusicXML validation failure still returns the file (True, path) because even non-strictly-valid MusicXML may be usable by Dorico -- the validation warning is logged
- Lazy import of `assemble_musicxml` inside `render()` method to avoid heavy music21 import at RenderPipeline construction time
- json_assembler registers instrument identifiers both from the explicit instrument_names list and from SectionNotation.instrument fields found in the data, handling LLM naming variations

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Installed xmlschema dependency**
- **Found during:** Task 1 setup
- **Issue:** xmlschema not installed -- `ModuleNotFoundError: No module named 'xmlschema'`
- **Fix:** `uv add xmlschema` installed 4.3.1 with elementpath 5.1.1 dependency
- **Files modified:** pyproject.toml, uv.lock
- **Verification:** `import xmlschema` succeeds, XSD schema loads, validation passes
- **Committed in:** 0529d37 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Expected dependency installation. No scope creep.

## Issues Encountered
- MusicXML 4.0 XSD file download required correct GitHub tag URL (`v4.0` not `refs/heads/v4.0`) -- resolved by testing URL format

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- MusicXML assembly pipeline fully connects Plan 01 (data layer) and Plan 02 (JSON fan-out) to the render pipeline
- Plan 04 can integrate end-to-end testing with real LLM-generated JSON data
- score.musicxml appears at top level in output ZIP alongside conductor-score.pdf
- --no-musicxml flag allows users to opt out of MusicXML generation

---
## Self-Check: PASSED
