---
phase: 07.1-minimal-ui-for-uat-needs
task: parallel-dispatch-and-prefix-cache
status: in_progress
last_updated: 2026-02-26T01:57:28.632Z
---

<current_state>
## Phase 07.1 complete (3/3 plans done)
- 07.1-03-SUMMARY.md created — TESTPLAN.md already existed from prior session
- Phase 07.1 is fully done

## Parallel dispatch + prefix cache: Step 1 of 5 complete
- PipelineConfig added to settings.py, engrave.toml, conftest.py MINIMAL_TOML
- Implementation plan: `/Users/patbarnson/.claude/plans/snug-mapping-pudding.md`
</current_state>

<completed_work>
This session:
1. Phase 07.1 Plan 03 closed out — TESTPLAN.md already existed, wrote 07.1-03-SUMMARY.md
2. PipelineConfig model added (max_concurrent_groups=8):
   - `src/engrave/config/settings.py`: new PipelineConfig class + pipeline field on Settings
   - `engrave.toml`: added `[pipeline] max_concurrent_groups = 8`
   - `tests/conftest.py`: added `[pipeline] max_concurrent_groups = 8` to MINIMAL_TOML
</completed_work>

<remaining_work>
## Step 2: Prompt restructuring for prefix cache (NEXT)
- `src/engrave/generation/prompts.py`: Change `build_section_prompt()` return type from `str` to `list[dict]`
  - Split into: system (RULES + DEFINITIVE), user1 (CONTEXTUAL), assistant (ack), user2 (variable: coherence + template + RAG + MIDI)
- `src/engrave/generation/pipeline.py`: Update `generate_section()` — currently wraps prompt in `[{"role": "user", "content": prompt}]`, change to pass messages list directly
- `_request_json_notation()`: Accept messages list, replace last user message with JSON variant
- `tests/unit/test_prompt_budget.py`: Update assertions — currently checks `prompt` as str (e.g. `"RULES" in prompt`), must check across messages list

## Step 3: Parallel group dispatch
- `src/engrave/generation/pipeline.py`: Extract group loop body (lines 596-693) into `_process_group()` async helper with `_GroupResult` dataclass
- Replace serial `for group in generation_groups:` with `asyncio.gather(*coros)` guarded by `asyncio.Semaphore(max_concurrent_groups)`
- Add `max_concurrent_groups` param to `generate_from_midi()`
- Update callers: `cli.py` generate command, `smoke/runner.py`, `web/app.py`

## Step 4: Smoke runner parallelization
- `src/engrave/smoke/runner.py`: Extract per-input into `_run_single_input()`, replace serial loop with `asyncio.gather`

## Step 5: Test updates + verification
- `uv run pytest tests/ -x` — all tests pass
- Integration test message format assertions may need updating
</remaining_work>

<decisions_made>
- PipelineConfig is a separate Pydantic model (not embedded in another config) under `[pipeline]` TOML section
- max_concurrent_groups=8 matches vllm-mlx slot count
- Prompt restructuring returns OpenAI-format messages list (list[dict]) not str
- Prefix boundary: system + user1 + assistant1 ≈ 700 tokens cached (vs 265 today)
- Sections remain sequential (coherence propagates), groups within sections parallelize
- _inject_no_think() in router.py already handles multi-message correctly — no router changes needed
</decisions_made>

<blockers>
None — all design decisions made, implementation plan approved.
</blockers>

<files_changed>
This session (uncommitted):
- `.planning/phases/07.1-minimal-ui-for-uat-needs/.continue-here.md` (updated)
- `.planning/phases/07.1-minimal-ui-for-uat-needs/07.1-03-SUMMARY.md` (new)
- `src/engrave/config/settings.py` (PipelineConfig added)
- `engrave.toml` ([pipeline] section added)
- `tests/conftest.py` ([pipeline] added to MINIMAL_TOML)

Prior uncommitted (from previous sessions):
- `data/corpus_db/chroma.sqlite3`, `pyproject.toml`, `src/engrave/cli.py`
- `src/engrave/generation/assembler.py`, `src/engrave/llm/router.py`
- `src/engrave/smoke/runner.py`, `tests/unit/test_router.py`, `uv.lock`
</files_changed>

<next_action>
/clear then implement Step 2: Prompt restructuring for prefix cache.

Start with `src/engrave/generation/prompts.py`:
1. Change `build_section_prompt()` return type to `list[dict]`
2. Split content into 4 messages: system(rules+hints), user1(audio context), assistant(ack), user2(variable)
3. Update `_request_json_notation()` in pipeline.py to accept messages list and swap last user message
4. Update `generate_section()` to pass messages directly (remove wrapping)
5. Fix tests in `test_prompt_budget.py` — assertions check across messages list

Full plan: `/Users/patbarnson/.claude/plans/snug-mapping-pudding.md`
</next_action>
