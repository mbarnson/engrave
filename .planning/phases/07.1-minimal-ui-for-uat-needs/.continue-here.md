---
phase: 07.1-minimal-ui-for-uat-needs
task: parallel-dispatch-and-prefix-cache
status: complete
last_updated: 2026-02-26
---

<current_state>
## All 5 steps COMPLETE — 907 tests pass

Steps 1-5 of parallel dispatch + prefix cache optimization are fully implemented and tested.
</current_state>

<completed_work>
All steps completed this session:

1. **PipelineConfig** (prior session): max_concurrent_groups=8 in settings/toml/conftest
2. **Prompt restructuring**: `build_section_prompt()` returns `list[dict]` (4 messages: system, user, assistant, user) for vllm-mlx prefix caching (~700 tokens cached vs 265)
3. **Parallel group dispatch**: `_process_group()` async helper + `_GroupResult` dataclass, `asyncio.gather` with Semaphore in `generate_from_midi()`, callers updated (cli.py, app.py, runner.py)
4. **Smoke runner parallelization**: `_run_single_input()` + `asyncio.gather` for concurrent input processing
5. **Test updates**: unit tests updated for list[dict] return, integration test mocks fixed (last user msg for var extraction, all content for assertions), 907/907 pass
</completed_work>

<remaining_work>
None — all implementation complete. Ready to commit.

Optional next steps:
- Real inference validation run (check vllm-mlx logs for higher cached token counts)
- Timing comparison (section processing time should drop ~4x)
</remaining_work>

<decisions_made>
- PipelineConfig under `[pipeline]` TOML section, max_concurrent_groups=8
- Prompt returns OpenAI-format messages list: system(rules+hints), user(audio), assistant(ack), user(variable)
- `_request_json_notation()` reuses prefix messages, replaces last user msg content
- Integration test mocks: iterate to LAST user msg (no break) for var extraction
- `test_audio_generation.py` mock captures ALL message content combined for assertion checks
- Sections remain sequential (coherence propagates), groups within sections parallelize
</decisions_made>

<files_changed>
- `src/engrave/generation/prompts.py` — build_section_prompt returns list[dict]
- `src/engrave/generation/pipeline.py` — _GroupResult, _process_group(), asyncio.gather, max_concurrent_groups param
- `src/engrave/cli.py` — pass max_concurrent_groups from settings
- `src/engrave/web/app.py` — pass max_concurrent_groups from settings
- `src/engrave/smoke/runner.py` — _run_single_input(), parallel dispatch, pass max_concurrent_groups
- `tests/unit/test_prompt_budget.py` — rewritten for list[dict] message format
- `tests/integration/test_audio_generation.py` — mock captures all content, no break
- `tests/integration/test_generation_fanout.py` — no break in msg iteration
- `tests/integration/test_midi_generation_steps.py` — no break in msg iteration
- `tests/integration/test_section_group_generation.py` — no break in msg iteration
- `tests/conftest.py` — no break in mock msg iteration
</files_changed>

<next_action>
Commit this work, then proceed to Phase 8 (Engraving Polish) or real inference validation.
</next_action>
