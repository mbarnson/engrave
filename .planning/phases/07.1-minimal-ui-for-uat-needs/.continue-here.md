---
phase: 07.1-minimal-ui-for-uat-needs
task: e2e-validation
total_tasks: 5
status: in_progress
last_updated: 2026-02-25T08:25:00.000Z
---

<current_state>
Code fixes committed (1e7a6c3). 907 tests passing.
Render WORKING: Big Band.zip produced (20 files: 1 PDF + 17 part .ly + score.ly + music-defs).
Web UI running on localhost:8000, processing inmood.mid (job d5273d18).
LLM generation is SLOW (~5-10 min per section on 30B model).
</current_state>

<completed_work>
Code fixes (all committed in 1e7a6c3):
1. _clef_for_instrument(): bass/trombone->bass, drums->percussion, bari->bass
2. Paper: explicit 431.8mm x 279.4mm (tabloid landscape, no rotation issues)
3. Layout: CONDUCTOR_SCORE_LAYOUT from stylesheet
4. \global in every staff for key/time/tempo
5. _deduplicate_dynamics(): strips consecutive identical dynamics
6. _MANGLED_CMD_RE fix: negative lookahead for dynamics+staccato (\f-.)
7. 907 tests passing

Pipeline validation:
- E2E generate bigband.mid: 12/12 sections, 0 errors (previous session)
- E2E render: Big Band.zip with 1.2MB PDF + 17 part .ly files + score.ly
- Web UI: /engrave endpoint accepts upload, returns processing status, htmx polling
- LMStudio: qwen/qwen3-coder-30b connected, API key in .env
</completed_work>

<remaining_work>
## Active
1. Web UI processing inmood.mid (job d5273d18 on localhost:8000)
   - Check: curl -s http://localhost:8000/status/d5273d18
   - When done: curl -s http://localhost:8000/download/d5273d18 -o inmood.zip
   - Server running as background process, may need restart: uv run engrave serve

## After web UI completes
2. Download ZIP from web UI and verify contents
3. Run CLI generate on inmood.mid: uv run engrave generate test-fixtures/inmood.mid --output jobs/e2e-test/inmood/
4. Run CLI generate on chattanooga.mid: uv run engrave generate test-fixtures/chattanooga.mid --output jobs/e2e-test/chattanooga/
5. Smoke test: uv run engrave smoke-test test-fixtures/ --json jobs/e2e-test/smoke-results.json

## DoD checklist (.planning/quick/end-to-end-real-inference-validation.md)
- [x] 1. engrave check generator passes
- [x] 2. engrave generate on bigband.mid produces LilyPond (12/12 sections)
- [x] 3. engrave render produces ZIP with real PDFs
- [ ] 4. engrave generate on chattanooga.mid (or inmood.mid) also works e2e
- [ ] 5. engrave smoke-test passes ALL 9 checks on BOTH files
- [ ] 6. Smoke test JSON saved to jobs/e2e-test/smoke-results.json
- [x] 7. Web UI accepts upload and starts processing (in progress)
- [x] 8. No mocks — real LLM, real compilation, real PDFs

## Known issues
- LLM generation is SLOW: ~5-10 min per section, 12 sections = 60+ min per MIDI
  Consider: skip JSON notation request to halve LLM calls (pipeline.py line 254)
- Drum notation: percussion clef but pitched notes
- Variable name mismatch: standalone fallback works
- LMStudio API key: ENGRAVE_PROVIDERS__LM_STUDIO__API_KEY in .env
</remaining_work>

<next_action>
1. Wait for web UI job d5273d18 to complete
2. Download and verify the ZIP from web UI
3. Run remaining MIDI files through CLI
4. Run smoke test harness
</next_action>
