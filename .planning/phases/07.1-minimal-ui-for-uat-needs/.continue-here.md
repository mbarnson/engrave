---
phase: 07.1-minimal-ui-for-uat-needs
task: e2e-validation
total_tasks: 5
status: in_progress
last_updated: 2026-02-25T06:55:00.000Z
---

<current_state>
E2E pipeline PROVEN: MIDI -> LLM generate -> LilyPond compile -> PDF -> ZIP.
All 12 sections generated with zero compile errors. PDF is 1.2MB with real notation.

Three bugs fixed this session (committed af98e4e):
1. Assembler sanitizer: strips mangled LilyPond commands (\time-., \ke-.y, \se-.t)
2. CLI render fallback: standalone compilation when vars don't match BigBandPreset
3. Auto-discovery of .ly files when music-definitions.ly absent

Previous session bugs (committed ede0c12):
4. Double variable declaration (strip_variable_wrapper + fallback parse)
5. LilyPond reserved word collision (drums -> drumsPart)
</current_state>

<completed_work>
- Phase 07.1 Plans 01-03: code-complete
- E2E generate: 12/12 sections, 0 errors, bigband.mid with qwen3-coder-30b
- E2E render: standalone compile produces 1.2MB PDF in ZIP
- Assembler sanitization: _MANGLED_CMD_RE, _CLEAN_GLOBAL_RE, _BARE_OPEN_BRACE_RE
- CLI render: fallback path with auto .ly discovery + standalone compile + ZIP
- Tests: 907 passing (pre-existing)
- Artifacts: jobs/e2e-test/bigband5/Big Band.zip, bigband-clean.pdf
</completed_work>

<remaining_work>
## DoD items still needed
1. Run tests to verify assembler/CLI changes don't break anything
2. Second MIDI file (chattanooga.mid) through full pipeline
3. Smoke test: uv run engrave smoke-test test-fixtures/ --json jobs/e2e-test/smoke-results.json
4. Web UI test: uv run engrave serve -> upload MIDI -> download ZIP
5. Re-run generate WITH assembler fix to produce clean .ly

## Known issues (not blocking E2E)
- Variable name mismatch: generate produces section-group vars (trumpets)
  but render expects individual vars (trumpetOne). Standalone fallback works.
- Parallel group dispatch deferred (user: "correctness first then parallel")
</remaining_work>

<next_action>
1. uv run pytest -- verify no regressions
2. Re-generate bigband.mid with assembler fix active
3. Continue through DoD checklist (second MIDI, smoke test, web UI)
</next_action>
