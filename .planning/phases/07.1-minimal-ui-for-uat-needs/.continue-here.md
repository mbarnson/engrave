---
phase: 07.1-minimal-ui-for-uat-needs
task: e2e-validation
total_tasks: 5
status: in_progress
last_updated: 2026-02-25T08:00:00.000Z
---

<current_state>
All 3 quality issues from user PDF review FIXED in code. 907 tests passing.
E2E generate running in background (bigband6/) with all assembler fixes active.
Next: compile output, verify PDF quality, run inmood.mid, smoke test, web UI.

Commits this session:
- af98e4e: assembler sanitizer + CLI render standalone fallback
- c617ce5: state checkpoint
- (pending): assembler quality fixes (clef, paper, layout, dynamics, global ref)
Previous session: ede0c12 (double-wrap + reserved words)
</current_state>

<completed_work>
- E2E generate: 12/12 sections, 0 errors, bigband.mid with qwen3-coder-30b
- E2E render: standalone compile -> 1.2MB PDF in ZIP
- Assembler sanitizer: strips mangled commands, bare braces, misplaced globals
- CLI render: fallback standalone compile path when vars don't match BigBandPreset
- 907 tests passing after all fixes

Quality fixes implemented:
1. Clef assignments: _clef_for_instrument() maps bass/trombone->bass, drums->percussion, bari->bass
2. Paper: explicit 431.8mm x 279.4mm (17"x11") instead of 'landscape flag (rotation fix)
3. Layout: CONDUCTOR_SCORE_LAYOUT imported from stylesheet (staff size 14, RemoveEmptyStaves, etc)
4. Global reference: \global now included in every staff ({ \clef X \global \varName })
5. Dynamic deduplication: _deduplicate_dynamics() strips consecutive identical dynamics
6. Mangled cmd regex fix: negative lookahead excludes dynamics+staccato (\f-.) from false positive
</completed_work>

<remaining_work>
## Immediate
1. Wait for bigband6 generate to complete
2. Compile and verify PDF quality (clef, paper, dynamics all correct)
3. Run engrave render on bigband6 output
4. Run engrave generate + render on inmood.mid

## DoD items still needed
5. Smoke test: uv run engrave smoke-test test-fixtures/ --json jobs/e2e-test/smoke-results.json
6. Web UI test: uv run engrave serve -> upload MIDI -> download ZIP
7. Second MIDI (chattanooga.mid) through pipeline

## Known issues
- Drum notation: percussion clef but pitched notes (needs \drummode, deeper fix)
- Variable name mismatch: section-group vars vs BigBandPreset individual vars (standalone fallback works)
</remaining_work>

<next_action>
1. Check bigband6 generate output
2. Compile with lilypond, verify PDF
3. Run inmood.mid through pipeline
4. Smoke test + web UI test
</next_action>
